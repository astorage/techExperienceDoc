应用层：向应用程序展示网络服务。类似Java mail，socket
表示层：session，检查点，应用程序之间同步
会话层（session）：管理通信。自动处理下层功能，对上层提供简单操作
传输层：TCP/UDP，数据拆分（如果数据太大），完整性，正确性，排序数据包

网络层：网络设备的路由功能，数据包中的ip（硬件：路由器，网桥路由，网关）
数据链路层：保证二进制的正确转换，错误检测纠正，建立数据链路（硬件：网卡、集线器、网桥）
物理层：二进制流——》电信号（模数、数模转换）（硬件：集线器，发送器，接收器，电缆，连接器，中继器）

